generator client {
  provider = "prisma-client-js"
  engineType = "library"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://testing_owner:rUJwt9BWA1Mj@ep-wild-surf-a1fdeawu.ap-southeast-1.aws.neon.tech/testing?sslmode=require"
}

model User {
  id       Int     @id @default(autoincrement())
  username String?
  email    String  @unique
  password String
}

model Todo {
  id          Int     @id @default(autoincrement())
  title       String?
  done        Boolean @default(false)
  description String
  userId      Int
}
